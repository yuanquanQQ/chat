# 项目领域词汇

本文只记录业务概念，不记录具体框架或部署细节。

## 用户与组织

- **用户（User）**：可以登录系统并参与会话的公司成员。用户名唯一；真实姓名、头像和部门是资料的一部分。
- **管理员（Administrator）**：被授予审核、账号管理和受授权排查权限的用户。
- **部门（Department）**：用户所属的组织单元。用户转部门不会直接生效，必须经过申请和管理员审批。
- **部门变更申请（Department Change Request）**：用户提交的目标部门、理由和审核结果。

## 沟通

- **会话（Conversation）**：消息的容器，分为单聊、群聊和预留的 AI 会话。
- **成员（Member）**：被加入会话的用户；群聊成员拥有群主、管理员或普通成员角色。
- **消息（Message）**：会话中的一条内容，可能是文本、图片、视频、音频、文件或系统事件。
- **撤回（Retraction）**：发送者在发送后五分钟内隐藏消息正文，其他成员看到“消息已撤回”；撤回不等于删除关联文件。
- **已读（Read Receipt）**：某用户已经读到某条消息的事实。单聊展示已读；群聊第一期只展示未读数量。

## 文件

- **文件元数据（File Metadata）**：文件名、大小、类型、哈希、发送者、会话和存储位置等信息，始终保存在服务器。
- **服务器文件（Server File）**：实际内容保存在公司服务器对象存储中的文件，发送者离线时仍可用。
- **客户端文件（Client File）**：实际内容保存在发送者电脑中的大文件；发送者离线时不可用，用户可选择转存为服务器文件。
- **文件预览（Preview）**：只读地展示常见图片、视频、音频、PDF、文本内容，不执行文件。

## 外部连接

- **AI 中枢（AI Center）**：未来与一个指定会话连接的外部系统。第一期只预留转发 seam，不启用业务逻辑。
- **审计日志（Audit Log）**：记录审核、登录、文件访问和受授权会话查看等管理动作，不等于默认允许查看所有聊天内容。

